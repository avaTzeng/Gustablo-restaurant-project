const globalReset = false;
const delayBase = 200;

export default {
    'CONTENT_UPWARD': {
        delay: delayBase,
        distance: '3rem',
        origin: 'bottom',
        reset: globalReset,
        easing: 'cubic-bezier(0.23, 1, 0.32, 1)'
    }
}